<section>
  <div class="best-seller">
    <div class="best-seller-container">
      <div class="container">
          <div class="best-seller--collection-title">
            <h2 class="heading">{{ section.settings.title }}</h2>
          </div>
        <div class="best-seller-content">
          <div class="best--seller--main--content product--slider-by-section-id" 
            data-slide-dots="{{ section.settings.show_dots }}"
            data-slide-arrows="{{ section.settings.show_arrows }}"
            data-slide-infinite="{{ section.settings.infinite }}"
            data-slide-auto_slide="{{ section.settings.auto_slide }}"
            data-slide-auto_speed="{{ section.settings.auto_speed }}"
            data-slide-slide_to_show="{{ section.settings.slide_to_show }}"
          >   
            {% for product in collections[section.settings.feature_collection].products %}
              <div class="product--item--colunm-padding"> 
                <div class="product--item--col product-item-hover">
                  {% render 'card-product', 
                    product:product,
                    cart_icon: section.settings.cart_icon,
                    show_second_img: section.settings.show_second_img,
                    enable_wishlist: section.settings.enable_wishlist,
                    enable_quick_view: section.settings.enable_quick_view
                  %}    
                </div>
              </div>
           {% endfor %}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
{% schema %}
  {
    "name": "featured-collection",
    "settings": [
    {
 		"type": "text",
 		"id": "title",
 		"label": "Chose Collection Name"
 	},
    {
 		"type": "collection",
 		"id": "feature_collection",
 		"label": "Chose Collection"
 	},
	{
      "type": "checkbox",
      "id": "cart_icon",
      "label": "Enable Cart Icon",
      "default": false
    },
	{
      "type": "checkbox",
      "id": "show_add_to_cart",
      "label": "Show add to cart button",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_second_img",
      "label": "Show second image on hover",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_wishlist",
      "label": "Enable Wishlist icon",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_quick_view",
      "label": "Enable Quick View",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_view_more",
      "label": "Show view more button",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_dots",
      "label": "Show slider dots",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_arrows",
      "label": "Show slider arrows",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "infinite",
      "label": "slider infinite",
      "default": true
    },
    {
      "type": "range",
      "id": "slide_to_show",
      "min": 0,
      "max": 5,
      "step": 1,
      "label": "Slide to show in desktop",
      "default": 3
    },
    {
      "type": "checkbox",
      "id": "auto_slide",
      "label": "Autoplay Slide",
      "default": false
    },
    {
      "type": "range",
      "id": "auto_speed",
      "min": 1000,
      "max": 5000,
      "step": 500,
      "label": "Autoplay Speed",
      "default": 2000
    }
],
"presets": [
      {
        "name": "featured-collection",
        "category": "text"
      }
    ]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}


